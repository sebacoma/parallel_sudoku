[package]
name = "parallel_sudoku"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
num_cpus = "1"
crossbeam-channel = "0.5"
crossbeam-utils = "0.8"
anyhow = "1"